#ifndef COURS__H  // Garde conditionnelle : si la variable DATE__H n'existe pas
#define COURS__H  // La déclarer.
#include "salle.h"
#include "enseignant.h"

struct cours{
	Date debut;
	Date fin;
	char* nom;
};
typedef struct cours cours;

void initialiser_cours(cours *c, salle s, enseignant e);

#endif // on clot la garde conditionne à la toute fin du fichier date.h
