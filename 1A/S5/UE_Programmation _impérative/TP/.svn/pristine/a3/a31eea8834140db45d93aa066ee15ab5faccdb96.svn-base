#ifndef SALLE__H  // Garde conditionnelle : si la variable DATE__H n'existe pas
#define SALLE__H  // La déclarer.
#include "date.h"

struct salle {
	char *nom;
	char *batiment;
};
typedef struct salle salle;
#endif // on clot la garde conditionne à la toute fin du fichier date.h
